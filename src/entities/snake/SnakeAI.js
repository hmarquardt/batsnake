// @ts-check
export class SnakeAI { constructor(){this.decision=1+Math.random()*2;this.aggression=.35+Math.random()*.4;}update(dt,controller,target){this.decision-=dt;if(!target){controller.advance(dt);return;}controller.aiUpdate(target.position,dt);const distance=controller.headPosition.distanceTo(target.position);if(this.decision<=0&&distance<10&&controller.strike.state==='idle'&&Math.random()<this.aggression){controller.strike.prepare(.35+Math.random()*.5);controller.strike.release(controller.headPosition,controller.direction);this.decision=2.2+Math.random()*3;}if(controller.awareness>.5)this.decision-=dt*.8;} }
