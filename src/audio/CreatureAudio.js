// @ts-check
/** Creature sound facade for future licensed sample sets. */
export class CreatureAudio { constructor(audio){this.audio=audio;}flap(position){this.audio.flap(position);}strike(position){this.audio.strike(position);} }
