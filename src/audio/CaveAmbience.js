// @ts-check
export class CaveAmbience {constructor(audio){this.audio=audio;this.dripTimer=2;this.wingTimer=.7;this.airTimer=1;}update(dt,camera,director=null){const density=director?.density||.18;this.dripTimer-=dt;this.wingTimer-=dt;this.airTimer-=dt;if(this.dripTimer<=0){this.dripTimer=3+Math.random()*7;this.audio.tone({x:(Math.random()-.5)*18,y:9,z:camera.position.z+(Math.random()-.5)*35},360,.32,'sine',.025,'effects');}if(this.wingTimer<=0&&density>.25){this.wingTimer=Math.max(.08,.8-density*.55)+Math.random()*.35;this.audio.tone({x:(Math.random()-.5)*12,y:4+Math.random()*5,z:camera.position.z+5+Math.random()*18},72+Math.random()*35,.07,'triangle',.012+density*.012,'creatures');}if(this.airTimer<=0&&camera.position.z>20){this.airTimer=1.2;this.audio.tone({x:0,y:7,z:50},48,.7,'sine',.015+Math.max(0,camera.position.z-20)*.0006,'effects');}}}
