// @ts-check
export class CaveAmbience {
  constructor(audio){this.audio=audio;this.dripTimer=2;this.wingTimer=.7;this.airTimer=1;this.insectTimer=1.8;this.resonanceTimer=4;}
  update(dt,camera,director=null){const z=camera.position.z,density=director?.density||.18,roost=Math.max(0,1-Math.abs(z+43)/24),mouth=Math.max(0,Math.min(1,(z-18)/32)),deep=1-mouth;this.dripTimer-=dt;this.wingTimer-=dt;this.airTimer-=dt;this.insectTimer-=dt;this.resonanceTimer-=dt;if(this.dripTimer<=0){this.dripTimer=2.7+Math.random()*5.5;const wetZ=Math.random()<.5?4:-25;this.audio.tone({x:(Math.random()<.5?-1:1)*(7+Math.random()*3),y:8+Math.random()*4,z:wetZ+(Math.random()-.5)*12},330+Math.random()*90,.28,'sine',.018+deep*.009,'effects');}if(this.wingTimer<=0&&(density>.25||roost>.3)){this.wingTimer=Math.max(.08,.8-density*.55)+Math.random()*.35;this.audio.tone({x:(Math.random()-.5)*12,y:10+Math.random()*4,z:-49+Math.random()*18},72+Math.random()*35,.07,'triangle',(.009+density*.012)*(.4+roost),'creatures');}if(this.airTimer<=0&&mouth>.05){this.airTimer=1.1;this.audio.tone({x:0,y:7,z:52},44+mouth*12,.7,'sine',.009+mouth*.028,'effects');}if(this.insectTimer<=0&&mouth>.35){this.insectTimer=.65+Math.random()*1.4;this.audio.tone({x:(Math.random()-.5)*14,y:3+Math.random()*6,z:55+Math.random()*9},2200+Math.random()*1300,.045,'sine',.004+mouth*.006,'effects');}if(this.resonanceTimer<=0&&deep>.55){this.resonanceTimer=5+Math.random()*8;this.audio.tone({x:(Math.random()-.5)*16,y:2,z:z+12},42+Math.random()*16,1.1,'sine',.006+deep*.007,'effects');}}
}
