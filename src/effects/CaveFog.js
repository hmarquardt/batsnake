// @ts-check
import * as THREE from 'three';
export class CaveFog { /** @param {THREE.Scene} scene */ constructor(scene){this.scene=scene;this.normalColor=new THREE.Color(0x050a08);this.thermalColor=new THREE.Color(0x080302);this.backgroundNormal=new THREE.Color(0x020504);this.backgroundThermal=new THREE.Color(0x010101);scene.fog=new THREE.FogExp2(this.normalColor,.026);scene.background=this.backgroundNormal.clone();}setThermal(active){this.setThermalBlend(active?1:0);}setThermalBlend(blend){if(this.scene.fog){this.scene.fog.color.copy(this.normalColor).lerp(this.thermalColor,blend);this.scene.fog.density=THREE.MathUtils.lerp(.026,.038,blend);}this.scene.background.copy(this.backgroundNormal).lerp(this.backgroundThermal,blend);} }
