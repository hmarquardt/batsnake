// @ts-check
import * as THREE from 'three';
export class ImpactEffects { /** @param {THREE.Scene} scene */ constructor(scene){this.scene=scene;this.effects=[];} burst(position,color=0xbad5ca){const mesh=new THREE.Mesh(new THREE.RingGeometry(.1,.18,20),new THREE.MeshBasicMaterial({color,transparent:true,opacity:.8,side:THREE.DoubleSide,depthWrite:false,blending:THREE.AdditiveBlending}));mesh.position.copy(position);this.scene.add(mesh);this.effects.push({mesh,life:.45});}update(dt,camera){for(let i=this.effects.length-1;i>=0;i--){const effect=this.effects[i];effect.life-=dt;effect.mesh.lookAt(camera.position);effect.mesh.scale.multiplyScalar(1+dt*10);effect.mesh.material.opacity=Math.max(0,effect.life*2);if(effect.life<=0){this.scene.remove(effect.mesh);effect.mesh.geometry.dispose();effect.mesh.material.dispose();this.effects.splice(i,1);}}} }
