// @ts-check
export class PerformanceOverlay {
  /** @param {HTMLElement} element @param {import('../core/Settings.js').Settings} settings */
  constructor(element,settings){this.element=element;this.settings=settings;this.elapsed=0;this.frames=0;this.fps=0;this.sensoryAverage=0;this.renderAverage=0;}
  update(dt,renderer,counts){this.element.classList.toggle('hidden',!this.settings.get('showPerformanceOverlay'));this.elapsed+=dt;this.frames++;this.sensoryAverage+=(counts.sensoryMs-this.sensoryAverage)*.08;this.renderAverage+=(counts.renderMs-this.renderAverage)*.08;if(this.elapsed>.5){this.fps=this.frames/this.elapsed;this.elapsed=0;this.frames=0;const info=renderer.info.render,routes=Object.entries(counts.routes||{}).map(([id,n])=>`${id[0]}:${n}`).join(' ');const resources=counts.resources,diagnostic=resources?`\n${resources.programs} programs · ${resources.materials} materials · ${resources.rendererGeometries}/${resources.geometries} GPU/scene geometries\necho ${resources.echoPool} · thermal ${resources.thermalPool} · sensory prewarm ${resources.prewarm?'ready':'cold'}`:'';this.element.textContent=`${this.fps.toFixed(0)} FPS · ${(1000/Math.max(1,this.fps)).toFixed(1)} ms frame · ${counts.input}\n${this.renderAverage.toFixed(2)} ms render · ${this.sensoryAverage.toFixed(3)} ms sensory · ${(counts.directorMs||0).toFixed(3)} ms director\n${info.calls} calls · ${(info.triangles/1000).toFixed(1)}k tris · ${renderer.info.memory.textures} textures · ${counts.renderTargets} RTs\n${counts.bats} bats · ${counts.panic} panic · ${routes}\nseed ${counts.seed||'—'} · ${counts.phase} · snakes ${(counts.alertness||0).toFixed(2)}${diagnostic}`;}}
}
